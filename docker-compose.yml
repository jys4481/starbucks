services:
  mysql:
    image: mysql:8.0.42
    environment:
      MYSQL_ROOT_PASSWORD: "0508"
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes: 
  mysql_data: