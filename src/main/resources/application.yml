spring:
  datasource:
    url: jdbc:mysql://localhost:3307/jpabegin?characterEncoding=utf8
    username: jpauser
    password: jpapass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # 개발환경: update / 운영 시 validate 권장
    properties:
      hibernate:
        format_sql: true
    show-sql: true